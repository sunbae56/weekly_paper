##위클리페이퍼 7

긴 영화 리뷰 문장의 감성(긍정/부정)을 분류하는 모델에 기본 RNN을 사용했어요. 
그런데 문장 맨 앞에 나온 중요한 단어의 정보가, 문장 끝에서 예측할 때는 거의 반영되지 않는 것 같아요. 어떤 원인을 의심할 건가요? 어떻게 해결할 건가요?

위의 모델은 many to one 을 사용하는 모델임
many to one 은 감성 분석(sentiment analysis)등을 함
단어들에서 하나의 결과를 판단함, 입력은 여러개의 문장 시퀀스가 되고 출력은 둘 중 하나. 긍정이냐 부정이냐가 된다.




히든스테이트: 과거의 현상인 이전 시점의 입력값들이 반영됨, 기억할 수 있는 구조
기존의 RNN은 단기기억임,장기기억에 약함. 이 단점을 보완한게 LSTM

기본적으로 딥러닝 학습에서는 레이어를 지날수록 새로운 정보를 반영하고 초깃값에 대한영향이 점점감소함
그래디언트가 전파될수록 멀리떨어진 부분의 정보는 점점 0에가깝게 줄어들거나 폭증하면서 정보 소실됨
RNN에서는 타임스텝에 따라 긴 입력 데이터를 처리하는데, 멀리 떨어진 정보관의 연관성을 Long Term dependency 라고함
RNN에서는 모든 시점에서 동일한 웨이트를 사용함, 거듭 웨이트를 곱해 나갈수록 초기의 정보가 소실될 수 있다 
단기기억은 잘하지만 긴시간은 약함
이를 해결하기 위해선 많은 레이어를 거치는 앞쪽의 선행 정보를 얼마나 뒤로 잘 전달하느냐에 있다
RNN에서 그 아이디어는 시간에 따라 정보가 줄거나 늘어나지 않는 어떤 경로를 추가하는 것인데
계속 동일한 웨이트를 곱하는게 가장 관건임 이 경로에선 w를 시점에 따라 적절히 조정해서 약간의 출렁거림을 추가하는 방식으로 해결하고자 함
이를 통해 선행정보를 손실없이 전달할수 있다. 이 조절이 가능한 부분을 게이트임
이 구조를 추가한 네트워크를 롱숏텀메모리라고함, 단기기억이긴한데 장기기억으로도 전환되는것
 


언어모델: 자연어의 기본적인 단위(예:단어) 로 이루어진 시퀀스를 확률로 표현하여 언어를 모델링

ex) 이 단어 다음에 어떤 단어가 오는 게 자연스러운가를 수식으로 나타낸 것

 앞선 언어의 시퀀스를 바탕으로 뒤에 언어질 언어모델을 만들때는 RNN
 

|얌전한|고양이가|부뚜막에|먼저|?|
|----|------|------|--|-|
|w1|w2|w3|w4|w5|


RNN의 한계, 장기기억에는 약함
