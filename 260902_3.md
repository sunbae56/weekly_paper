

위클리페이퍼 3

##질문1
🇶 model.fit()으로 학습을 돌렸는데, loss 값이 처음엔 잘 줄어들다가 어느 순간부터 점점 커지더니 결국 NaN이 되어버렸어요. 
어떤 원인들을 의심해볼 건가요? 각 원인을 어떻게 확인할 건가요?

>솔직히 이 질문은 내가 이해하기 어려운 질문 이었고 아예 모르는 범위의 질문이었기 때문에 이유를 찾아가고 내가 이해하는 과정을 쓰기로 했다.

 
## 값이 커지다가 결측치가 발생하는 원인

   1. 오버플로(Overflow)


* 변수가 표현할 수 있는 최대값보다 데이터가 더 커질 때 발생합니다.
* 컴퓨터가 이 값을 감당하지 못하면 무한대(Inf)로 바꾸거나 시스템 안전을 위해 결측치(NaN)로 처리합니다.


   1. 수학적 연산 오류


* 값이 이미 너무 커져서 무한대(Inf)가 된 상태에서 추가 연산을 할 때 발생합니다.
* 대표적으로 무한대끼리 빼거나 나누는 연산(Inf - Inf, Inf / Inf)을 수행하면 컴퓨터가 계산할 수 없어 NaN(Not a Number)을 반환합니다.


   1. 자료형(Data Type) 한계


* 정수형(Int)이나 실수형(Float) 등 각 자료형이 가질 수 있는 메모리 용량 한계를 초과하는 경우입니다.
* 데이터의 데이터 타입이 허용 범위를 넘어서면 유효한 값을 상실하고 시스템에 의해 Null이나 NaN으로 강제 변환될 수 있습니다.


   1. 센서 및 외부 시스템 한계 (내가 알 수 있는 범위의 내용이 아님)

* 물리적인 센서나 측정 장비, 혹은 외부 API 데이터 수집 단계에서 발생합니다.
* 기기가 측정할 수 있는 최대 범위를 초과하는 수치가 들어오면, 에러를 내는 대신 신호 유실(Blank) 처리되어 결측치로 기록됩니다.


##제미나이는 이렇게 대답했다.
##하지만 나는 오버플로 정의와 자료형 한계의 경우의 경우를 자세히 알지 못했다

오버플로:
머신러닝(ML)에서 오버플로(Overflow)는 모델이 표현할 수 있는 숫자의 최댓값을 초과하여 값이 무한대(Infinity)나 NaN(Not a Number)으로 망가지는 현상을 말합니다. 

1. 주요 원인

* 큰 입력값이나 이상치: 데이터에 극단적인 이상치가 포함되어 있거나 입력값이 너무 클 경우
* 지수 함수 연산: Softmax 등에서 exp() 함수를 계산할 때 입력값이 커지면 결과가 급격히 폭발
* 기울기 폭발(Gradient Explosion): 신경망 층이 깊거나 학습률이 너무 클 때 역전파 과정에서 가중치 값이 계속 곱해지며 한계치를 초과



##하지만 나는 또 다시 지수 함수 연산이 무엇인지를 몰라서 찾아보았다 

1. 지수 함수 연산이란 무엇인가요?
어떤 수를 지정된 횟수만큼 반복해서 곱하는 계산을 말합니다.
머신러닝(특히 딥러닝)에서는 자연상수 'e'(약 2.718)라는 숫자를 밑으로 두고, 입력값 'x'만큼 거듭제곱하는 'e의 x제곱(exp(x))' 연산을 주로 사용합니다.
2. 왜 머신러닝에서 지수 함수를 쓰나요?

* 미분이 아주 잘 되어서 인공지능이 학습하기 좋습니다.
* 마이너스(-) 숫자가 입력되어도 결과값을 항상 플러스(+) 양수로 바꿔줍니다.
* Softmax 함수 등에서 각 선택지의 확률(비율)을 뚜렷하게 구별해 주는 효과가 있습니다.

1. 왜 이 연산 때문에 오버플로가 발생하나요?
지수 함수는 숫자가 커지는 속도가 세상에서 가장 빠른 연산 중 하나입니다.

* x가 1일 때: 2.7
* x가 10일 때: 약 2만 2천
* x가 20일 때: 약 4억 8천만
* x가 89일 때: 컴퓨터(float32 데이터 타입)가 저장할 수 있는 우주적인 최댓값(약 3.4 곱하기 10의 38제곱)을 넘어가 버립니다.

따라서 입력값 x가 겨우 90만 되어도 컴퓨터는 숫자를 감당하지 못하고 무한대(Inf)나 에러(NaN)를 뱉어내게 됩니다.



1. 해결 방안

* 클리핑(Clipping): 가중치나 기울기(Gradient)가 특정 범위를 넘지 못하도록 강제로 상한선을 두어 제한
* 수치 안정화 기법: Softmax 연산 시 입력값에서 최댓값을 빼주는 방식을 사용하여 지수 함수의 크기를 제한
* 정규화(Normalization): 입력 데이터를 사전에 정규화하거나 스케일링하여 숫자의 범위를 안정적으로 유지

##하지만 다시, 나는 어떤때에 모델이 표현할 수 있는 숫자의 최댓값이 초과하는지 
활성화 함수가 무엇인지 언제 연산 결과가 너무 커지는지 도 모른다 


머신러닝에서 모델이 표현할 수 있는 숫자의 최댓값(데이터 타입의 한계)을 초과하는 상황은 크게 4가지 케이스로 요약할 수 있습니다.
컴퓨터는 보통 32비트 실수형(float32)으로 숫자를 저장하는데, 이 형식이 버틸 수 있는 최대 숫자는 약 3.4 × 10³⁸입니다. 연산 결과가 이보다 커지면 즉시 무한대(Inf)나 계산 불능(NaN) 상태가 됩니다.
구체적으로 다음과 같은 상황에서 발생합니다.
## 1. 지수 함수($e^x$)에 큰 입력값이 들어갈 때
딥러닝 모델의 출력층에서 가장 자주 발생하는 원인입니다.

* 발생 위치: 주로 Softmax(소프트맥스)나 Sigmoid(시그모이드) 활성화 함수 내부
* 초과 원인: 지수 함수 $e^x$는 입력값이 조금만 커져도 결과값이 기하급수적으로 폭발합니다.
* 한계 도달: float32 기준으로 입력값 x가 89만 넘어도 e⁸⁹는 약 4.4 × 10³⁸이 되어 최댓값을 초과(Overflow)해 버립니다.

## 2. 역전파 과정에서 기울기가 폭발할 때 (Gradient Explosion)
심층 신경망을 학습시키는 과정에서 발생합니다.

* 발생 위치: 순환 신경망(RNN)이나 층이 아주 깊은 신경망의 가중치 업데이트 과정
* 초과 원인: 역전파(Backpropagation) 시 여러 레이어를 거치며 미분값(기울기)을 반복해서 곱하게 됩니다.
* 한계 도달: 각 레이어의 기울기가 1보다 조금만 커도(예: 1.5), 이를 50번, 100번 연속으로 곱하면(1.5¹⁰⁰) 숫자가 천문학적으로 커져 제한 범위를 넘어섭니다.

## 3. 학습률(Learning Rate)을 지나치게 높게 설정했을 때
모델의 하이퍼파라미터를 잘못 설정했을 때 발생합니다.

* 발생 위치: 가중치 업데이트 공식: 가중치 = 가중치 - (학습률 × 기울기)
* 초과 원인: 학습률이 너무 크면 모델이 최적의 값을 찾아가지 못하고 보폭을 너무 크게 디딥니다.
* 한계 도달: 매 학습 단계마다 가중치 값이 수렴하지 않고 오히려 발산하며 커지다가, 결국 표현 가능한 최댓값을 뚫고 올라갑니다.

## 4. 입력 데이터 정규화를 안 했거나 극단적인 이상치가 있을 때
데이터 전처리 단계에서 미흡했을 때 발생합니다.

* 발생 위치: 모델의 첫 번째 은닉층(Hidden Layer) 연산 구역
* 초과 원인: 수억 단위의 대형 숫자 데이터나 가공되지 않은 이상치(Outlier)가 그대로 모델에 입력되는 경우입니다.
* 한계 도달: 이 큰 입력 데이터가 초기 가중치와 곱해지고 더해지는 순간, 첫 번째 레이어의 출력값부터 최댓값을 초과해 버릴 수 있습니다.

 

1. 활성화 함수(Activation Function)가 무엇인가요?
인공신경망의 뇌 세포 역할을 하는 수학 공식입니다. 입력된 데이터들의 합을 넘겨받아 다음 층으로 신호를 보낼 때, 이 신호를 그대로 보낼지 아니면 특정 값으로 변환해서 보낼지 결정합니다. 특히 직선(선형)이 아닌 곡선(비선형) 형태로 신호를 바꾸어 줌으로써, 모델이 복잡한 패턴을 학습할 수 있게 만듭니다.
2. 활성화 함수에서 언제 연산 결과가 너무 커지나요?
컴퓨터가 계산하기 버거울 정도로 숫자가 폭발하는 순간은 다음과 같습니다.

* Softmax나 Sigmoid 같은 함수에서 입력값(x) 자체의 크기가 89 이상으로 커질 때: 이 함수들은 내부적으로 'e의 x제곱(지수 함수)'을 계산합니다. 입력값 x가 조금만 커져도 결과값은 기하급수적으로 커져서 32비트 컴퓨터가 표현할 수 있는 한계치(약 3.4 곱하기 10의 38제곱)를 순식간에 돌파합니다.
* ReLU 함수를 쓸 때 데이터 정규화가 안 되었을 때: ReLU는 입력값이 양수이면 그 값을 그대로 통과시키는 함수입니다. 입력 데이터의 단위가 너무 크거나(예: 수억 단위), 학습 도중 가중치가 계속 곱해지면서 숫자가 커지면 제한 없이 무한대로 값이 커지며 오버플로가 발생합니다.

