위클리페이퍼 6

이미지 분류 모델을 완전연결층(Fully Connected Layer)만으로 만들었더니 파라미터 수가 수백만 개를 넘고, 사진 속 물체 위치가 조금만 바뀌어도 예측이 크게 달라져요.
왜 이런 문제가 생기고, CNN은 이 문제를 어떻게 해결하는지 설명해 주세요.


완전연결층은 
층마다의 모든 뉴런이 다 연결되있음 
모든 픽셀을 일렬로 펴서 층에 넣음
모든 입력을 서로 연결해서 학습하고 위치정보는 없다. 파라미터수가 많아진다

cnn은 이미지같이 공간적 구조가 있으면서 데이터를 처리하는 신경망이다.

|구분	|완전연결층(FC) |	CNN|
|-----|------------|--------------------------|
|연결 방식|	모든 뉴런을 연결|	주변의 지역적인 영역을 연결 
|이미지 공간정보	|잘 활용하지 못함|	잘 활용함 |
|파라미터|	많아지기 쉬움	|상대적으로 적음 |
|주요 역할|	정보를 종합하여 분류|	이미지의 특징 추출에 강함 |
|예	|마지막 분류층	| 이미지 인식 전체 구조|
 
문제가 생기는 이유: fcn은 모든 입력과 출력이 서로 연결되기 때문에 파라미터가 생기기 쉬워짐

cnn은 필터(커널)를 사용한다 .예를들어 이미지가 10x10이면 fcn은 10000개의 픽셀이 필요하고 거기에 가중치랑 bias도 포함해서 계산해야하지만 
cnn은 3x3 필터를 사용한다고 하면 컬러일때는 33x3x32 = 864개가 된다
이미지 공간정보를 활용해서 필터가 이미지 정보를 돌아가며 특징을 추출한다.
그러나 fcn은 특징맵이 없다. 자리정보가 없고 모든 픽셀들을 flatten해서 계산한다.
이미지에서 중첩되는 특징을 활용할수 없거나 특징 추출을 하지 못한다.



 


 
