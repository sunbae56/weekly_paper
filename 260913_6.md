위클리페이퍼 6

이미지 분류 모델을 완전연결층(Fully Connected Layer)만으로 만들었더니 파라미터 수가 수백만 개를 넘고, 사진 속 물체 위치가 조금만 바뀌어도 예측이 크게 달라져요.
왜 이런 문제가 생기고, CNN은 이 문제를 어떻게 해결하는지 설명해 주세요.



CNn으로는 한계가 있다
1. 파라미터 수 증가로 인한 문제
• 모든 픽셀을 연결하기 때문에 파라미터 수가 폭발적으로 증가
• 예: 100x100 이미지 → 1만 픽셀
• 첫 은닉층을 1천 노드로 설정 시 1천만 개의 연결 필요
• 결과:
• 과적합 위험 → 일반화 성능 저하
• 계산 비용 증가 → 학습 및 추론 속도 저하
• 확장성 낮음 → 고해상도 이미지 처리에 비효율적
(예: 1024x1024x3 이미지 → 3백만 입력 노드 필요)
2. 지역 정보 손실(Local Information Loss)
• 픽셀 간 공간적 구조(예: 인접 픽셀의 관계) 반영 불가
• 이미지를 1차원 벡터로 변환→패턴(예: 가장자리, 모서리) 학습 어려움 


이럴땐 풀링 연산을 쓸 수 있다.

